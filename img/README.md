# img/

Put your **profile photo** here, named exactly `profile.jpg`.

The home page (`index.html`) shows it as a circular photo in the header.
If the file is missing, a crimson "JG" initials circle is shown instead —
so the site looks fine either way.

A roughly square image (e.g. 600×600 px or larger) works best.
