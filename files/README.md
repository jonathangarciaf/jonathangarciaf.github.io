# files/

Put your **CV** here, named exactly `cv.pdf`.

The CV page (`cv.html`) links to and embeds `files/cv.pdf`. Until the file
exists, the page shows a "not added yet" placeholder instead.

When you update it, also change the "Last updated" date near the top of `cv.html`.
